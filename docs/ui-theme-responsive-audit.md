# Forge UI Theme And Responsive Audit

This document tracks the active view-by-view audit for theme responsiveness and screen-size responsiveness. It is not a substitute for fixing code; it is the checklist that prevents losing scope while the fixes proceed.

Audit rules:

- Every routed view must be checked at desktop and mobile widths.
- Every page-owned component and imported UI component listed below must either respond to theme tokens or be explicitly justified as intentionally fixed-tone media/canvas chrome.
- Hardcoded dark-only utility colors such as `text-white`, `bg-white/*`, `border-white/*`, raw black panels, and raw rgba focus/border styles are audit targets unless already covered by a compatibility bridge.
- Fixed-width tables, minimum-width rows, non-wrapping action bars, absolute overlays, graph controls, and modal footers are responsive audit targets.
- A row is only `checked` when the live route has been inspected and no ugly horizontal overflow or theme-incoherent component remains for that route.

## Current High-Priority Findings

| Route                         | Component                                 | Finding                                                                                                                                                                                                                     | Status                    |
| ----------------------------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| `/settings/agents`            | `SettingsAgentsPage` agent console cards  | Runtime sessions, session history, reconnect commands, agent tokens, connected agents, and active projects used desktop row layouts or unbreakable strings that overflowed mobile screens.                                  | fixed                     |
| `/training-load`              | `ZoneIntelligencePanel` and session table | Mobile rendered dense fixed-column zone and recent workout tables; both now use mobile card layouts below `md`.                                                                                                             | fixed                     |
| `/projects/:projectId`        | evidence activity cards                   | Project detail activity/evidence links could expand to desktop widths on mobile because the cards and text lacked shrink/wrap boundaries.                                                                                   | fixed                     |
| `/sports/workouts/:workoutId` | workout HR chart and detail cards         | Dense heart-rate samples shared same-minute X labels, producing duplicate React keys in Recharts. Detail rows also kept hardcoded dark styles and weak wrapping around provider labels.                                     | fixed                     |
| shared detail pages           | `EntityNotesSurface`                      | Goal, project, task, and Psyche detail pages used a shared note panel with hardcoded white/dark surfaces, note pills, preview panels, select controls, and action buttons.                                                  | fixed in shared component |
| all routes                    | shared dark utility classes               | Many pages still use hardcoded `text-white`, `bg-white/*`, `border-white/*`, and raw rgba styles. Some are temporarily covered by the global light-theme bridge, but the correct end state is component-level tokenization. | open                      |

## Routed View Inventory

|   # | Route                                       | Page                               | Source                                               | Theme/responsive components to audit                                                                                                                                                                                                                                                                                                                                 | Status                              |
| --: | ------------------------------------------- | ---------------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
|   1 | `/` -> `/overview`                          | `OverviewPage`                     | `src/pages/overview-page.tsx`                        | AiSurfaceWorkspace, Badge, Card, EmptyState, EntityBadge, EntityName, EntityNoteCountLink, FlagshipSignalDeck, GamificationOverviewWidget, InfoTooltip, LifeForceOverviewWorkspace, MiniCalendarWidget, PageHero, ProgressMeter, QuickCaptureWidget, SpotifyWidget, TimeWidget, WeatherWidget                                                                        | theme/mobile checked                |
|   2 | `/overview`                                 | `OverviewPage`                     | `src/pages/overview-page.tsx`                        | AiSurfaceWorkspace, Badge, Card, EmptyState, EntityBadge, EntityName, EntityNoteCountLink, FlagshipSignalDeck, GamificationOverviewWidget, InfoTooltip, LifeForceOverviewWorkspace, MiniCalendarWidget, PageHero, ProgressMeter, QuickCaptureWidget, SpotifyWidget, TimeWidget, WeatherWidget                                                                        | theme/mobile checked                |
|   3 | `/life-force`                               | `LifeForcePage`                    | `src/pages/life-force-page.tsx`                      | LifeForceOverviewWorkspace, PageHero, UserScopeSelector                                                                                                                                                                                                                                                                                                              | theme/mobile checked                |
|   4 | `/goals`                                    | `GoalsPage`                        | `src/pages/goals-page.tsx`                           | Badge, Button, Card, CardHeaderRow, EmptyState, EntityBadge, EntityName, EntityNoteCountLink, GoalStudio, PageHero, PillCluster, ProgressMeter, ProjectCollectionFilters, UserBadge                                                                                                                                                                                  | theme/mobile checked                |
|   5 | `/habits`                                   | `HabitsPage`                       | `src/pages/habits-page.tsx`                          | Badge, Button, Card, EmptyState, EntityName, EntityNoteCountLink, GamificationMiniHud, HabitDialog, NoteMarkdown, PageHero, SelectMenu, Textarea, UserBadge                                                                                                                                                                                                          | theme/mobile checked                |
|   6 | `/goals/:goalId`                            | `GoalDetailPage`                   | `src/pages/goal-detail-page.tsx`                     | Badge, Button, Card, EmptyState, EntityBadge, EntityName, EntityNotesSurface, GoalDialog, NoteMarkdown, OpenInGraphButton, PageHero, PreferenceEntityHandoffButton, ProgressMeter, ProjectCollectionFilters, ProjectDialog, UserBadge                                                                                                                                | theme/mobile checked                |
|   7 | `/projects`                                 | `ProjectsPage`                     | `src/pages/projects-page.tsx`                        | AiSurfaceWorkspace, Badge, Button, Card, EmptyState, EntityBadge, EntityName, EntityNoteCountLink, PageHero, PillCluster, ProgressMeter, ProjectCollectionFilters, ProjectManagementSectionNav, ProjectSearchBar, ProjectsHeroBox, ProjectsSearchResultsBox, ProjectsSummaryBox, SectionGrid, UserBadge                                                              | theme/mobile checked                |
|   8 | `/projects/hierarchy`                       | `ProjectManagementHierarchyPage`   | `src/pages/project-management-hierarchy-page.tsx`    | Badge, Button, Card, EmptyState, EntityBadge, EntityLinkMultiSelect, ErrorState, LoadingState, PageHero, ProjectManagementSectionNav, Tree, UserBadge                                                                                                                                                                                                                | theme/mobile checked                |
|   9 | `/projects/:projectId`                      | `ProjectDetailPage`                | `src/pages/project-detail-page.tsx`                  | Badge, Button, Card, EntityBadge, EntityName, EntityNotesSurface, ErrorState, ExecutionBoard, GamificationMiniHud, NoteMarkdown, OpenInGraphButton, PageHero, PreferenceEntityHandoffButton, ProjectDialog, ProjectManagementSectionNav, SchedulingRulesEditor, SurfaceSkeleton, TaskDialog, UserBadge, WorkAdjustmentDialog                                         | theme/mobile checked                |
|  10 | `/strategies`                               | `StrategiesPage`                   | `src/pages/strategies-page.tsx`                      | Badge, Button, Card, EmptyState, Input, PageHero, StrategyDialog, UserBadge                                                                                                                                                                                                                                                                                          | theme/mobile checked                |
|  11 | `/strategies/:strategyId`                   | `StrategyDetailPage`               | `src/pages/strategy-detail-page.tsx`                 | Badge, Button, Card, ErrorState, LoadingState, OpenInGraphButton, PageHero, PreferenceEntityHandoffButton, ProgressMeter, StrategyDialog, StrategyGraphCanvas, StrategyHierarchyTree, UserBadge                                                                                                                                                                      | theme/mobile checked                |
|  12 | `/preferences`                              | `PreferencesPage`                  | `src/pages/preferences-page.tsx`                     | Badge, Button, Card, DimensionBar, EmptyState, ErrorState, Input, LoadingState, PageHero, PreferenceGameDialog, PreferenceWorkspaceControls, PreferenceWorkspaceTabNav, PsycheSectionNav, Textarea, UserBadge                                                                                                                                                        | theme/mobile checked                |
|  13 | `/calendar`                                 | `CalendarPage`                     | `src/pages/calendar-page.tsx`                        | Badge, Button, CalendarEventFlowDialog, CalendarQuickRenameDialog, CalendarWeekToolbar, Card, EntityBadge, ErrorState, FloatingActionMenu, PageHero, SurfaceSkeleton, TaskSchedulingDialog, TimeboxPlanningDialog, WorkBlockFlowDialog                                                                                                                               | theme/mobile checked                |
|  14 | `/knowledge-graph`                          | `KnowledgeGraphPage`               | `src/pages/knowledge-graph-page.tsx`                 | Button, EntityBadge, EntityLinkMultiSelect, ErrorState, FacetedTokenSearch, GamificationMiniHud, Input, KnowledgeGraphEntityPanel, KnowledgeGraphForceView, KnowledgeGraphHierarchyView, LoadingState, ModalCloseButton, SheetScaffold, UserBadge                                                                                                                    | theme/mobile checked                |
|  15 | `/movement`                                 | `MovementPage`                     | `src/pages/movement-page.tsx`                        | Badge, Button, Card, ErrorState, FacetedTokenSearch, InfoTooltip, Input, MovementDataBrowserBox, MovementLifeTimeline, MovementPlaceEditorDialog, MovementPlacesBox, MovementSelectionBox, MovementSummaryBox, MovementTimelineBox, PageHero, SheetScaffold, SurfaceSkeleton                                                                                         | theme/mobile checked                |
|  16 | `/sleep`                                    | `SleepPage`                        | `src/pages/sleep-page.tsx`                           | Badge, Button, Card, EntityLinkMultiSelect, ErrorState, Input, PageHero, PsycheSectionNav, SleepBrowserBox, SleepPatternsBox, SleepSummaryBox, SurfaceSkeleton, Textarea                                                                                                                                                                                             | theme/mobile checked                |
|  17 | `/sports`                                   | `SportsPage`                       | `src/pages/sports-page.tsx`                          | Badge, Button, Card, EntityLinkMultiSelect, ErrorState, FacetedTokenSearch, Input, PageHero, SheetScaffold, SportsBrowserBox, SportsCompositionBox, SportsSummaryBox, SurfaceSkeleton, Textarea                                                                                                                                                                      | theme/mobile checked                |
|  18 | `/sports/workouts/:workoutId`               | `WorkoutDetailPage`                | `src/pages/workout-detail-page.tsx`                  | Badge, Button, Card, ErrorState, Input, PageHero, SurfaceSkeleton, Textarea                                                                                                                                                                                                                                                                                          | theme/mobile checked                |
|  19 | `/training-load`                            | `TrainingLoadPage`                 | `src/pages/training-load-page.tsx`                   | Badge, Card, ChartBox, ErrorState, InfoTooltip, PageHero, SurfaceSkeleton, ZoneIntelligencePanel                                                                                                                                                                                                                                                                     | theme/mobile checked                |
|  20 | `/vitals`                                   | `VitalsPage`                       | `src/pages/vitals-page.tsx`                          | Badge, Card, ErrorState, LoadingState, MetricDetailSections, PageHero, SpotlightCard                                                                                                                                                                                                                                                                                 | theme/mobile checked                |
|  21 | `/weight-loss`                              | `WeightLossPage`                   | `src/pages/weight-loss-page.tsx`                     | ErrorState, LoadingState, PageHero, WeightLossActionPanel, WeightLossCheckinDialog, WeightLossDataQualityPanel, WeightLossExperimentsPanel, WeightLossFoodLogDialog, WeightLossHistoryDialog, WeightLossHypothesesPanel, WeightLossInsightMetric, WeightLossLedgerPanel, WeightLossNutritionTargetsPanel, WeightLossPlanDialog                                       | theme/mobile checked                |
|  22 | `/psyche`                                   | `PsychePage`                       | `src/pages/psyche-page.tsx`                          | AiSurfaceWorkspace, Badge, Button, Card, EditableSurface, EntityBadge, EntityName, ErrorState, GamificationMiniHud, InteractiveCard, PageHero, PsycheGraphCanvas, PsycheSectionNav, ReflectFlowDialog, SurfaceSkeleton                                                                                                                                               | theme/mobile checked                |
|  23 | `/psyche/metrics`                           | `PsycheMetricsPage`                | `src/pages/psyche-metrics-page.tsx`                  | Badge, Card, ErrorState, LoadingState, MetricDetailSections, PageHero, PsycheSectionNav, SpotlightCard                                                                                                                                                                                                                                                               | theme/mobile checked                |
|  24 | `/psyche/flashcards`                        | `PsycheFlashcardsPage`             | `src/pages/psyche-flashcards-page.tsx`               | Badge, Button, ErrorState, Input, LoadingState, PageHero, PsycheSectionNav, Textarea, UserBadge, UserSelectField                                                                                                                                                                                                                                                     | theme/mobile checked                |
|  25 | `/psyche/values`                            | `PsycheValuesPage`                 | `src/pages/psyche-values-page.tsx`                   | AtlasPanel, Button, EntityBadge, EntityName, EntityNoteCountLink, ErrorState, FlowField, Input, LoadingState, OrbitMap, PageHero, PsycheSectionNav, QuestionFlowDialog, Textarea, UserBadge, UserSelectField                                                                                                                                                         | theme/mobile checked                |
|  26 | `/psyche/patterns`                          | `PsychePatternsPage`               | `src/pages/psyche-patterns-page.tsx`                 | AtlasPanel, Badge, Button, EntityBadge, EntityLinkMultiSelect, EntityName, EntityNoteCountLink, ErrorState, FlowField, Input, LoadingState, OrbitMap, PageHero, PsycheSectionNav, QuestionFlowDialog, SchemaBadge, Textarea, UserBadge, UserSelectField                                                                                                              | theme/mobile checked                |
|  27 | `/psyche/questionnaires`                    | `PsycheQuestionnairesPage`         | `src/pages/psyche-questionnaires-page.tsx`           | Badge, Button, Card, EmptyState, ErrorState, FacetedTokenSearch, LoadingState, PageHero, PsycheSectionNav                                                                                                                                                                                                                                                            | theme/mobile checked                |
|  28 | `/psyche/questionnaires/new`                | `PsycheQuestionnaireBuilderPage`   | `src/pages/psyche-questionnaire-builder-page.tsx`    | Badge, Button, Card, EmptyState, ErrorState, LoadingState, PageHero, PsycheSectionNav                                                                                                                                                                                                                                                                                | theme/mobile checked                |
|  29 | `/psyche/questionnaires/:instrumentId`      | `PsycheQuestionnaireDetailPage`    | `src/pages/psyche-questionnaire-detail-page.tsx`     | Badge, Button, Card, EmptyState, ErrorState, LoadingState, PageHero, PsycheSectionNav                                                                                                                                                                                                                                                                                | theme/mobile checked                |
|  30 | `/psyche/questionnaires/:instrumentId/edit` | `PsycheQuestionnaireBuilderPage`   | `src/pages/psyche-questionnaire-builder-page.tsx`    | Badge, Button, Card, EmptyState, ErrorState, LoadingState, PageHero, PsycheSectionNav                                                                                                                                                                                                                                                                                | theme/mobile checked                |
|  31 | `/psyche/questionnaires/:instrumentId/take` | `PsycheQuestionnaireRunPage`       | `src/pages/psyche-questionnaire-run-page.tsx`        | Badge, Button, Card, ErrorState, LoadingState, PageHero                                                                                                                                                                                                                                                                                                              | theme/mobile checked                |
|  32 | `/psyche/questionnaire-runs/:runId`         | `PsycheQuestionnaireRunDetailPage` | `src/pages/psyche-questionnaire-run-detail-page.tsx` | Badge, Button, Card, EmptyState, ErrorState, LoadingState, PageHero                                                                                                                                                                                                                                                                                                  | theme/mobile checked                |
|  33 | `/psyche/self-observation`                  | `PsycheSelfObservationPage`        | `src/pages/psyche-self-observation-page.tsx`         | Badge, Button, CalendarWeekToolbar, Card, EntityBadge, EntityLinkMultiSelect, ErrorState, Input, NoteTagsInput, PageHero, PsycheSectionNav, SelectMenu, SheetScaffold, SurfaceSkeleton, Textarea, UserBadge, UserSelectField                                                                                                                                         | theme/mobile checked                |
|  34 | `/psyche/screen-time`                       | `PsycheScreenTimePage`             | `src/pages/psyche-screen-time-page.tsx`              | Badge, Button, Card, ErrorState, PageHero, PsycheSectionNav, SurfaceSkeleton                                                                                                                                                                                                                                                                                         | theme/mobile checked                |
|  35 | `/psyche/behaviors`                         | `PsycheBehaviorsPage`              | `src/pages/psyche-behaviors-page.tsx`                | AtlasPanel, Badge, Button, EntityLinkMultiSelect, EntityName, EntityNoteCountLink, ErrorState, FlowField, Input, LoadingState, PageHero, PsycheSectionNav, QuestionFlowDialog, ReturnPathStrip, SchemaBadge, Textarea, UserBadge, UserSelectField                                                                                                                    | theme/mobile checked                |
|  36 | `/psyche/reports`                           | `PsycheReportsPage`                | `src/pages/psyche-reports-page.tsx`                  | AtlasPanel, Badge, BehaviorRowsEditor, Button, EmotionRowsEditor, EmptyState, EntityLinkMultiSelect, EntityName, EntityNoteCountLink, ErrorState, FieldHint, FlowField, InfoTooltip, Input, LoadingState, ModeTimelineEditor, PageHero, PsycheSectionNav, QuestionFlowDialog, SchemaBadge, StringListEditor, Textarea, ThoughtRowsEditor, UserBadge, UserSelectField | theme/mobile checked                |
|  37 | `/psyche/reports/:reportId`                 | `PsycheReportDetailPage`           | `src/pages/psyche-report-detail-page.tsx`            | Badge, BehaviorRowsEditor, Button, ChainCanvas, EmotionRowsEditor, EntityBadge, EntityNotesSurface, ErrorState, FieldHint, InfoTooltip, Input, InsightFlowDialog, ModeTimelineEditor, OpenInGraphButton, PageHero, PsycheSectionNav, SchemaBadge, StringListEditor, SurfaceSkeleton, Textarea, ThoughtRowsEditor                                                     | theme/mobile checked                |
|  38 | `/psyche/goal-map`                          | `PsycheGoalMapPage`                | `src/pages/psyche-goal-map-page.tsx`                 | Badge, Card, EntityBadge, EntityName, ErrorState, LoadingState, PageHero, PsycheGraphCanvas, PsycheSectionNav                                                                                                                                                                                                                                                        | theme/mobile checked                |
|  39 | `/psyche/schemas-beliefs`                   | `PsycheSchemasBeliefsPage`         | `src/pages/psyche-schemas-beliefs-page.tsx`          | AtlasPanel, Badge, Button, EntityBadge, EntityLinkMultiSelect, EntityNoteCountLink, ErrorState, FieldHint, FlowField, InfoTooltip, Input, LoadingState, PageHero, PsycheSectionNav, QuestionFlowDialog, SchemaBadge, Textarea, UserBadge, UserSelectField                                                                                                            | theme/mobile checked                |
|  40 | `/psyche/modes`                             | `PsycheModesPage`                  | `src/pages/psyche-modes-page.tsx`                    | AtlasPanel, Badge, Button, EntityBadge, EntityLinkMultiSelect, EntityName, EntityNoteCountLink, ErrorState, FlowField, Input, LoadingState, ModeChip, PageHero, PsycheSectionNav, QuestionFlowDialog, Textarea, UserSelectField                                                                                                                                      | theme/mobile checked                |
|  41 | `/psyche/modes/guide`                       | `PsycheModeGuidePage`              | `src/pages/psyche-mode-guide-page.tsx`               | Badge, Button, Card, EmptyState, ErrorState, LoadingState, PageHero, PsycheSectionNav                                                                                                                                                                                                                                                                                | theme/mobile checked                |
|  42 | `/kanban`                                   | `KanbanPage`                       | `src/pages/kanban-page.tsx`                          | Badge, Button, Card, EmptyState, EntityBadge, EntityLinkMultiSelect, ExecutionBoard, Input, KanbanBoardBox, KanbanFiltersBox, KanbanSummaryBox, PageHero, ProjectDialog, ProjectManagementSectionNav, TaskDialog, UserBadge                                                                                                                                          | theme/mobile checked                |
|  43 | `/notes`                                    | `NotesPage`                        | `src/pages/notes-page.tsx`                           | Badge, Button, Card, EmptyState, EntityLinkMultiSelect, ErrorState, FloatingActionMenu, Input, NoteComposerBox, NoteFilterInput, NoteFiltersBox, NoteMarkdown, NoteTagsInput, NotesLibraryBox, PageHero, Textarea                                                                                                                                                    | theme/mobile checked                |
|  44 | `/wiki`                                     | `WikiPage`                         | `src/pages/wiki-page.tsx`                            | Badge, Button, EmptyState, EntityBadge, ErrorState, GamificationMiniHud, Input, LoadingState, OpenInGraphButton, WikiArticleMarkdown, WikiIngestModal                                                                                                                                                                                                                | theme/mobile checked                |
|  45 | `/wiki/ingest-history`                      | `WikiIngestHistoryPage`            | `src/pages/wiki-ingest-history-page.tsx`             | Button, Card, EmptyState, ErrorState, Input, PageHero, SurfaceSkeleton, WikiIngestModal                                                                                                                                                                                                                                                                              | theme/mobile checked                |
|  46 | `/wiki/page/:slug`                          | `WikiPage`                         | `src/pages/wiki-page.tsx`                            | Badge, Button, EmptyState, EntityBadge, ErrorState, GamificationMiniHud, Input, LoadingState, OpenInGraphButton, WikiArticleMarkdown, WikiIngestModal                                                                                                                                                                                                                | theme/mobile checked                |
|  47 | `/wiki/new`                                 | `WikiEditorPage`                   | `src/pages/wiki-editor-page.tsx`                     | Badge, Button, EmptyState, EntityLinkMultiSelect, ErrorState, InfoTooltip, Input, LoadingState, Textarea, WikiArticleMarkdown                                                                                                                                                                                                                                        | theme/mobile checked                |
|  48 | `/wiki/edit/:pageId`                        | `WikiEditorPage`                   | `src/pages/wiki-editor-page.tsx`                     | Badge, Button, EmptyState, EntityLinkMultiSelect, ErrorState, InfoTooltip, Input, LoadingState, Textarea, WikiArticleMarkdown                                                                                                                                                                                                                                        | theme/mobile checked                |
|  49 | `/today`                                    | `TodayPage`                        | `src/pages/today-page.tsx`                           | AiSurfaceWorkspace, Badge, Card, DailyRunway, EmptyState, EntityNoteCountLink, LifeForceTodayCard, MiniCalendarWidget, PageHero, ProgressMeter, QuickCaptureWidget, SpotifyWidget, TimeWidget, TodayCalendarBox, TodayFocusBox, TodayHeroBox, TodayMetricsBox, TodayRunwayBox, WeatherWidget                                                                         | theme/mobile checked                |
|  50 | `/workbench`                                | `WorkbenchPage`                    | `src/pages/workbench-page.tsx`                       | Button, EntityBadge, FacetedTokenSearch, PageHero                                                                                                                                                                                                                                                                                                                    | theme/mobile checked                |
|  51 | `/workbench/:flowId`                        | `WorkbenchFlowPage`                | `src/pages/workbench-flow-page.tsx`                  | EmptyState, ErrorState, LoadingState, OpenInGraphButton, PageHero, WorkbenchFlowEditor                                                                                                                                                                                                                                                                               | theme/mobile checked                |
|  52 | `/activity`                                 | `ActivityPage`                     | `src/pages/activity-page.tsx`                        | ActivityTable, Card, EmptyState, ErrorState, LoadingState, PageHero                                                                                                                                                                                                                                                                                                  | theme/mobile checked                |
|  53 | `/insights`                                 | `InsightsPage`                     | `src/pages/insights-page.tsx`                        | ApplyInsightDialog, Badge, Button, Card, ErrorState, InfoTooltip, InsightFlowDialog, LoadingState, PageHero, UserBadge                                                                                                                                                                                                                                               | theme/mobile checked                |
|  54 | `/review/weekly`                            | `WeeklyReviewPage`                 | `src/pages/weekly-review-page.tsx`                   | Badge, Button, Card, ErrorState, FlagshipSignalDeck, PageHero, SurfaceSkeleton                                                                                                                                                                                                                                                                                       | theme/mobile checked                |
|  55 | `/settings`                                 | `SettingsPage`                     | `src/pages/settings-page.tsx`                        | Button, Card, ErrorState, Input, PageHero, SettingsSectionNav, SurfaceSkeleton, ThemeCustomizerDialog                                                                                                                                                                                                                                                                | theme/mobile checked                |
|  56 | `/settings/data`                            | `SettingsDataPage`                 | `src/pages/settings-data-page.tsx`                   | Badge, Button, Card, ErrorState, FlowChoiceGrid, FlowField, Input, MetricTile, PageHero, QuestionFlowDialog, SettingsSectionNav, SurfaceSkeleton                                                                                                                                                                                                                     | theme/mobile checked                |
|  57 | `/settings/users`                           | `SettingsUsersPage`                | `src/pages/settings-users-page.tsx`                  | Badge, Button, Card, Input, PageHero, SettingsSectionNav, UserBadge, UserRelationshipFlowDialog, UserRelationshipGraph, UserSettingsFlowDialog                                                                                                                                                                                                                       | theme/mobile checked                |
|  58 | `/settings/calendar`                        | `SettingsCalendarPage`             | `src/pages/settings-calendar-page.tsx`               | Badge, Button, CalendarConnectionFlowDialog, Card, ErrorState, PageHero, QuestionFlowDialog, SettingsSectionNav, SurfaceSkeleton                                                                                                                                                                                                                                     | theme/mobile checked                |
|  59 | `/settings/mobile`                          | `SettingsMobilePage`               | `src/pages/settings-mobile-page.tsx`                 | Badge, Button, Card, ErrorState, PageHero, SettingsSectionNav, SurfaceSkeleton                                                                                                                                                                                                                                                                                       | theme/mobile checked                |
|  60 | `/settings/mobile/lab`                      | `CompanionSyncLabPage`             | `src/pages/companion-sync-lab-page.tsx`              | Badge, Card, PageHero                                                                                                                                                                                                                                                                                                                                                | theme/mobile checked                |
|  61 | `/settings/models`                          | `SettingsModelsPage`               | `src/pages/settings-models-page.tsx`                 | Badge, Button, Card, ErrorState, LoadingState, PageHero, SettingsSectionNav                                                                                                                                                                                                                                                                                          | theme/mobile checked                |
|  62 | `/settings/agents`                          | `SettingsAgentsPage`               | `src/pages/settings-agents-page.tsx`                 | AgentTokenFlowDialog, Badge, Button, Card, EntityBadge, EntityName, ErrorState, LogWorkFlowDialog, MetricTile, PageHero, SettingsSectionNav, SurfaceSkeleton, TokenRevealDialog                                                                                                                                                                                      | theme/mobile checked                |
|  63 | `/settings/rewards`                         | `SettingsRewardsPage`              | `src/pages/settings-rewards-page.tsx`                | Badge, Button, Card, ErrorState, Input, MetricTile, PageHero, SettingsSectionNav, SurfaceSkeleton, Textarea, XpCommandDeck                                                                                                                                                                                                                                           | theme/mobile checked                |
|  64 | `/rewards`                                  | `RewardsPage`                      | `src/pages/rewards-page.tsx`                         | Badge, Button, Card, ErrorState, GamificationMiniHud, PageHero, ProgressMeter                                                                                                                                                                                                                                                                                        | theme/mobile checked                |
|  65 | `/settings/wiki`                            | `SettingsWikiPage`                 | `src/pages/settings-wiki-page.tsx`                   | Badge, Button, Card, ErrorState, InfoTooltip, Input, LoadingState, PageHero, SettingsSectionNav                                                                                                                                                                                                                                                                      | theme/mobile checked                |
|  66 | `/settings/logs`                            | `SettingsLogsPage`                 | `src/pages/settings-logs-page.tsx`                   | Badge, Button, Card, EntityBadge, ErrorState, Input, PageHero, SettingsSectionNav, SurfaceSkeleton                                                                                                                                                                                                                                                                   | theme/mobile checked                |
|  67 | `/settings/bin`                             | `SettingsBinPage`                  | `src/pages/settings-bin-page.tsx`                    | Button, Card, EntityBadge, ErrorState, Input, PageHero, SettingsSectionNav, SurfaceSkeleton                                                                                                                                                                                                                                                                          | theme/mobile checked                |
|  68 | `/tasks/:taskId`                            | `TaskDetailPage`                   | `src/pages/task-detail-page.tsx`                     | Badge, Button, Card, EntityName, EntityNotesSurface, ErrorState, GamificationMiniHud, InfoTooltip, NoteMarkdown, OpenInGraphButton, PageHero, PreferenceEntityHandoffButton, SheetScaffold, SurfaceSkeleton, TaskDialog, TaskSchedulingDialog, TimeboxPlanningDialog, WorkAdjustmentDialog                                                                           | theme/mobile checked                |

## Verification Log

- 2026-06-04: Reopened `/forge/settings/agents` because the row had only mobile evidence and the page/dialog surfaces still used dark-only white text, translucent white panels, raw RGB modal chrome, and fixed semantic status text. Tokenized `SettingsAgentsPage`, `AgentTokenFlowDialog`, `LogWorkFlowDialog`, and `TokenRevealDialog` with `--ui-*` surface, modal, overlay, ink, border, shadow, code, success, warning, danger, and primary tokens. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, fixed hex colors, dark custom shadows, and fixed Tailwind semantic color families in `src/pages/settings-agents-page.tsx`, `src/components/settings/agent-token-flow-dialog.tsx`, `src/components/settings/log-work-flow-dialog.tsx`, and `src/components/settings/token-reveal-dialog.tsx` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/settings/agents` in forced Paper light at 1365px and 390px across the base page, plus desktop Create token and Log work dialogs: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Reopened `/forge/notes` because the row had only mobile evidence and shared notes controls still had dark popovers, translucent white pills, white text, cyan fixed color chips, raw dark gradients, and raw dark custom shadows. Tokenized `EntityNoteCountLink`, `NoteFilterInput`, and `InlineNoteFields` with `--ui-*` surface, modal, ink, border, shadow, and info tokens. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, fixed hex colors, dark custom shadows, and fixed Tailwind semantic color families in `src/pages/notes-page.tsx` and `src/components/notes/**` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/notes` in forced Paper light at 1365px and 390px across the base page, visible filter input interaction, and New note composer: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Rechecked `/forge/weight-loss` after the Energy gap tooltip/formula work and route source scan. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, fixed hex colors, dark custom shadows, and fixed Tailwind semantic color families in `src/pages/weight-loss-page.tsx` and `src/components/weight-loss/**` returns no matches. Fixed `WeightLossNutritionTargetsPanel` so its macro/vitamin plus sport-loss/mineral split only goes side-by-side at `2xl`; at normal desktop widths the right column no longer clips past the visible viewport. `npx tsc --noEmit` passed. Live-verified `/forge/weight-loss` in forced Paper light at 1365px and 390px across the base page plus food, plan, and history modal paths: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Reopened `/forge/training-load` because the row had only mobile evidence and the page still used dark-only metric/table cards, translucent white controls, raw RGB Recharts grid/tick/tooltip styling, fixed chart hex colors, and dark zone-intelligence chrome. Tokenized route metric tiles, headings, workout cards/tables, Recharts grids/ticks/tooltips/series colors, intensity/zone fills, radar/charts, contribution cards, warning callouts, and `ZoneIntelligencePanel` mode/time controls, tables, cards, and chart surfaces with `--ui-*` surface, ink, border, success, warning, info, danger, tertiary, and primary tokens. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, fixed hex colors, dark custom shadows, and fixed Tailwind semantic color families in `src/pages/training-load-page.tsx` and `src/components/training-load/**` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/training-load` in forced Paper light at 1365px and 390px across the base page plus Month, 90d, Combat, Base, and Endurance toggle states: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Reopened `/forge/sleep` because the row had only mobile overflow evidence and `SleepPage` still used fixed dark gradients, black panels, white text, raw RGB sleep-stage colors, fixed Tailwind semantic status colors, and dark raw-data panels. Tokenized stage metadata, recovery/raw/source status badges, last-night hero, baseline cards, stage bars, phase rails, calendar cells, detail panel tabs, reflection fields, raw evidence cards, code block, and route fallback cards with `--ui-*` surface, ink, border, code, success, warning, info, danger, and primary tokens. Fixed shared `PsycheSectionNav` desktop active state so tiny active labels use `--ui-ink-strong` instead of weak tertiary orange on Paper. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, dark custom shadows, and fixed Tailwind semantic color families in `src/pages/sleep-page.tsx` and `src/components/psyche/psyche-section-nav.tsx` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/sleep` in forced Paper light at 1365px and 390px across the base page, Summary, Reflection, and Show raw data tabs: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Reopened `/forge/movement` because the row had only mobile overflow evidence and the route still contained dark-gradient cards, white/translucent badges, raw RGB chart/SVG colors, and dark-only movement timeline chrome. Tokenized `MovementPage`, `MovementTimelineViewportGrid`, `MovementTimelineHistoryCap`, `MovementTimelineRow`, and `MovementLifeTimeline` visible data-sheet chrome with `--ui-*` surface, ink, border, shadow, info, warning, and danger tokens. Fixed duplicate React keys for same-day projected missing movement boxes by de-duplicating the rendered day strip and using composite keys in day/data virtualized rows. Tightened mobile day-strip sizing and the timeline data sheet header so they no longer create hidden wide children. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, dark custom shadows, and fixed Tailwind semantic color families in `src/pages/movement-page.tsx` and `src/components/movement/**` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/movement` in forced Paper light at 1365px and 390px across Life, Day, Month, and All-time modes, plus Add place, timeline Actions, and timeline data modal: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Tokenized `/forge/preferences` overview/map/table/history/contexts/concepts surfaces and `PreferenceGameDialog` away from remaining dark-only/fixed semantic styling. Fixed preference-map point chips so positive/negative state stays in the semantic border/soft background while text uses `--ui-ink-strong` for Paper/light contrast. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, dark shadow utilities, and fixed Tailwind semantic color families in `src/pages/preferences-page.tsx`, `src/components/preferences/**`, and `src/components/users/**` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/preferences` in forced Paper light at 1365px and 390px across overview, map, table, history, contexts, and concepts tabs, plus the `Start the game` guided modal: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast controls.
- 2026-06-04: Rechecked `/forge/goals/goal_fc241c03d9` after tokenizing the remaining goal-detail fixed colors: `ProjectCollectionFilters` active state now uses `--ui-border-strong`, `--ui-accent-soft`, and `--ui-shadow-soft`; the goal delete action uses semantic danger tokens; the missing-goal fallback link uses `--ui-ink-on-accent`. Source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, dark shadow utilities, and fixed Tailwind color families in `src/pages/goal-detail-page.tsx` and `src/components/projects/project-collection-filters.tsx` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/goals/goal_fc241c03d9` in Paper/light at 1365px and 390px, including the base page plus `Edit life goal` and `Add project` guided dialogs: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast buttons.
- 2026-06-04: Tokenized `/forge/projects` project cards, compact search-result rows, empty-state action, summary metrics, spotlight card, status badges, and tag pills away from dark-only `white/*` utilities and fixed emerald/amber text. Focused source grep for hardcoded dark/light utilities, raw RGB/RGBA, raw gradients, dark shadow utilities, and fixed slate/zinc/gray status colors in `src/pages/projects-page.tsx`, `src/components/project-dialog.tsx`, and `src/components/projects/**` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/projects` in Paper/light at 1365px and 390px, including the base route, visible search input interaction, and mobile project-management section drawer: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast buttons.
- 2026-06-04: Tokenized `/forge/projects/hierarchy` search panel, controls card, owner/level/status badges, tree card, virtualized rows, expand controls, execution badges, assignee badges, progress rails, and open links away from dark-only gradients, `white/*` utilities, raw RGBA panels, fixed cyan/amber/indigo/emerald/rose status colors, and dark shadows. Dynamic entity accents remain content colors but are mixed with `--ui-surface-*` tokens instead of hardcoded black/white RGBA. Focused source grep for hardcoded dark/light utilities, raw RGBA, dark shadow utilities, and fixed Tailwind status colors returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/projects/hierarchy` in Paper/light at 1365px and 390px, including the base route, hierarchy search interaction, and mobile project-management section drawer: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast buttons.
- 2026-06-04: Tokenized `/forge/strategies` list/search surface away from dark-only search copy, strategy headings, status badges, metric cards, active-node chips, and alignment breakdown text. Focused source grep for hardcoded dark/light utilities, raw RGBA, dark shadow utilities, and fixed Tailwind status colors in `src/pages/strategies-page.tsx`, `src/components/strategy-dialog.tsx`, `src/components/strategy-graph-canvas.tsx`, and `src/components/strategy-hierarchy-tree.tsx` returns no matches. `npx tsc --noEmit` passed. Live-verified `/forge/strategies` in Paper/light at 1365px and 390px, including search interaction and the `New strategy` guided dialog: no failed non-SSE requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast buttons. The detail route remains non-live-data for now because `/forge/strategies` currently has zero strategy records.
- 2026-06-03: Inventory generated from `src/app.tsx` and page imports. Next implementation pass targets `/settings/agents` and `/training-load` because live mobile checks and source inspection found concrete overflow risks there.
- 2026-06-03: Fixed `/training-load` mobile overflow by replacing the `ZoneIntelligencePanel` dense zone table and the page-level recent workout table with mobile card layouts below `md`. Verified live on `http://127.0.0.1:4317/forge/training-load` at 390px: `bodyOverflow = 0`, no visible overflow offenders.
- 2026-06-03: Fixed `/settings/agents` mobile overflow in runtime session cards, session history rows, reconnect command blocks, token cards, connected agent cards, and active project cards. Verified live on `http://127.0.0.1:4317/forge/settings/agents` at 390px: `bodyOverflow = 0`, no visible overflow offenders.
- 2026-06-03: Fixed confirmed compact-list overflow in `/overview` pipeline cards and `/projects` compact search results by adding shrink boundaries and wrapping badges. Rebuilt the OpenClaw plugin runtime and verified live at 390px: `/forge/overview`, `/forge/projects`, `/forge/campaigns`, `/forge/life-force`, `/forge/goals`, `/forge/habits`, `/forge/projects/hierarchy`, `/forge/strategies`, `/forge/preferences`, and `/forge/knowledge-graph` all returned `bodyOverflow = 0` with no visible overflow offenders.
- 2026-06-03: `/forge/weight-loss` food logging flow was live-verified at 390px after making `Custom food`, `Ask ChatGPT`, and `Search food` explicit top-level actions; modal click-through had `bodyOverflow = 0`. Added a visible `Today active calories` panel that shows default active calories, observed same-day HealthKit/workout/movement/step evidence, the applied source, target formula, and editable daily override. Live-verified at 390px: saving 405 kcal changed the API source to `user_override` and target to 1517 kcal; reset removed the override, restored default 388 kcal and target 1500 kcal; `bodyOverflow = 0`.
- 2026-06-03: Tokenized `/notes` summary cards, composer/edit panels, ephemeral controls, note metadata, note link badges, and note action button away from hardcoded white/dark utility classes. Fixed shared `NoteMarkdown` so headings, paragraphs, lists, quotes, pills, inline code, and fenced code use theme tokens; long inline code now wraps with `overflow-wrap:anywhere` and fenced code stays constrained to its container. Also fixed `listFlashcards` to respect the entity-search API `limit <= 200`, eliminating `/api/v1/entities/search` 400s on notes. Live-verified `/forge/notes` at 390px: no runtime error, no failed responses, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Added Tailwind source exclusions for generated `dist`, OpenClaw plugin `dist`, and plugin runtime `dist` folders so generated CSS is not re-scanned as source. Removed the escaped wiki parent-picker selector that produced CSS optimizer warnings. Rebuilt `npm run build:openclaw-plugin` cleanly, restarted/verified the source-backed `4317` runtime, confirmed Vite markers `/forge/@vite/client` and `/forge/src/main.tsx`, confirmed the live database is `/Users/omarclaw/Documents/aurel-monorepo/data/forge/forge.sqlite`, and live-verified `/forge/weight-loss` and `/forge/notes` at 390px with no failed API responses, no console/page errors, and `bodyOverflow = 0`.
- 2026-06-03: Live-scanned the next detail/health batch at 390px. `/forge/projects/project_c65a26cd72` exposed mobile overflow in project evidence cards; fixed `ProjectDetailPage` activity links with shrink boundaries, tokenized surfaces/text, and `overflow-wrap:anywhere`. `/forge/sports/workouts/workout_edea95cc61` emitted duplicate React key warnings from same-minute HR chart labels; fixed `WorkoutDetailPage` to use unique numeric timestamps for chart X values, format labels only for display, tokenized route/zone/detail surfaces, and wrapped provider metric/event labels. Re-verified `/forge/projects/project_c65a26cd72` and `/forge/sports/workouts/workout_edea95cc61` at 390px: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders. Also scanned `/forge/goals/goal_fc241c03d9`, `/forge/movement`, `/forge/sleep`, `/forge/sports`, and `/forge/vitals` at 390px with no failed responses and `bodyOverflow = 0`; these remain theme-tokenization candidates because source grep still shows hardcoded dark utility styles in parts of the health/movement stack. `/strategies/:strategyId` could not be live-detail checked because `/api/v1/strategies` currently returns an empty list.
- 2026-06-03: Tokenized shared `EntityNotesSurface` away from hardcoded dark-only note panels, link pills, select controls, search chrome, preview boxes, ephemeral settings panels, metadata, and edit buttons. Added explicit `min-w-0`, `max-w-full`, `break-words`, and `overflow-wrap:anywhere` boundaries to linked-entity pills and note metadata so linked note content cannot force detail pages wider than mobile viewports. Live-verified `/forge/goals/goal_fc241c03d9`, `/forge/projects/project_c65a26cd72`, and `/forge/tasks/task_46d4e7129a` at 390px: the note surface mounted, no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders. `npx tsc --noEmit` and `npm run build:openclaw-plugin` passed; `4317` remained healthy, source-backed, and on `/Users/omarclaw/Documents/aurel-monorepo/data/forge/forge.sqlite`.
- 2026-06-03: Tokenized shared `DailyMetricsDashboard` away from dark-only chart, tooltip, fullscreen dialog, sparkbar, spotlight card, metric detail card, badge, and category-section styling. Recharts grid, axes, cursors, lines, dots, and gradients now use Forge theme variables; cards and dialog chrome use `--ui-surface-*`, `--ui-ink-*`, `--ui-border-*`, and semantic category variables. Source grep for `text-white`, `bg-white`, `border-white`, raw `rgba(`, and raw `rgb(` in `src/components/metrics/daily-metrics-dashboard.tsx` now returns no matches. Live-verified `/forge/vitals` and `/forge/psyche/metrics` at 390px: shared dashboard mounted, no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders. Opened one fullscreen metric dialog on each route at 390px: dialog opened, no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `/forge/psyche` hub surfaces away from dark-only styling in `PsychePage`, `PsycheSectionNav`, and `PsycheGraphCanvas`. The graph canvas now uses `--ui-*`/semantic tokens for the frame, controls, legend, SVG tone maps, node fills/strokes/text, inspector cards, fallback cards, and mobile section drawer. Mobile graph fit now applies wider padding and a lower fit scale so the live SVG starts within its frame instead of clipping node geometry against the viewport edge. Source grep for `text-white`, `bg-white`, `border-white`, raw `rgba(`, and raw `rgb(` in `src/pages/psyche-page.tsx`, `src/components/psyche/psyche-section-nav.tsx`, and `src/components/psyche/psyche-graph.tsx` returns no matches. Live-verified `/forge/psyche` at 1365px and 390px: no failed responses, no console/page errors, `bodyOverflow = 0`, no HTML overflow offenders, and after the graph fit patch no SVG overflow offenders.
- 2026-06-03: Tokenized `/forge/psyche/flashcards` card chrome, filter panel, badges, empty state, and editor modal away from hardcoded dark-only surfaces. User-selected flashcard preview colors remain content-specific, but the surrounding app UI now uses `--ui-surface-*`, `--ui-ink-*`, `--ui-border-*`, `--overlay`, semantic danger tokens, and standard shadow tokens. Also tokenized shared Psyche `AtlasPanel` and `OrbitMap`, which are used by values and patterns routes, replacing dark gradients, white text, raw rgba tone cards, and custom dark shadows with theme variables and wrapping boundaries. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, raw `rgba(`, raw `rgb(`, and raw dark shadow utilities in `src/pages/psyche-flashcards-page.tsx`, `src/components/psyche/atlas-panel.tsx`, and `src/components/psyche/orbit-map.tsx` returns no matches. Live-verified `/forge/psyche/flashcards` at 1365px and 390px, including opening the flashcard editor modal: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `/forge/psyche/values` and `/forge/psyche/patterns` route-local cards away from hardcoded white/dark utilities and raw rgba semantic blocks. Values now use tokenized value cards, why-it-matters panels, linked goal/project/task wells, wrapped entity labels, and success-toned committed-action chips. Patterns now use tokenized source-observation callouts, pattern cards, cue/loop/payoff/return-path lane cards, wrapped entity labels, and semantic danger/warning/success soft surfaces. Shared `EntityLinkMultiSelect`, `SchemaBadge`, and `psycheFocusClass` no longer use raw rgba/white/dark focus chrome in the targeted scan. `OrbitMap` now renders a stacked wrapped card list on mobile and keeps the constellation layout for tablet/desktop, eliminating mobile clipped absolute nodes. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, raw `rgba(`, raw `rgb(`, raw dark shadow utilities, and raw slate/violet styling in `src/pages/psyche-values-page.tsx`, `src/pages/psyche-patterns-page.tsx`, `src/components/psyche/orbit-map.tsx`, `src/components/psyche/entity-link-multiselect.tsx`, `src/components/psyche/schema-badge.tsx`, and `src/components/psyche/use-psyche-focus-target.ts` returns no matches. Live-verified `/forge/psyche/values` and `/forge/psyche/patterns` at 1365px and 390px, including opening the guided add-value/add-pattern dialogs: no failed responses, no console/page errors, `bodyOverflow = 0`. The only remaining intrinsic-width detector hit in those scans was the shared shell `Background activity` button, outside these route bodies.
- 2026-06-03: Tokenized `/forge/psyche/questionnaires` index cards, metadata blocks, badges, run-history wells, and bottom explainer cards away from hardcoded dark gradients, white text, and raw rgba badges. Also tokenized shared `FacetedTokenSearch`, replacing dark-only container/input/dropdown/selected-chip/action styling with `--ui-*` tokens and allowing long option labels/descriptions to wrap with `overflow-wrap:anywhere`. Source grep for hardcoded white/dark/raw rgba patterns in `src/pages/psyche-questionnaires-page.tsx` and `src/components/search/faceted-token-search.tsx` returns no matches. Live-verified `/forge/psyche/questionnaires` at 1365px and 390px, including typing into the filter input: no failed responses, no console/page errors, `bodyOverflow = 0`, and the previous mobile long-alias option overflow is gone. The only remaining intrinsic-width detector hit was the shared shell `Background activity` button, outside the route body.
- 2026-06-03: Tokenized `/forge/psyche/self-observation` route-local week cards, filter/export panels, observation cards, activity cards, sheet sections, mobile day cards, note tags, calendar week toolbar, and shared select menu away from hardcoded dark/white/raw RGB styling. Fixed the mobile export panel by giving constrained grid children and `SelectMenu` explicit `min-w-0`/`max-w-full` boundaries so intrinsic option labels cannot exceed the card width. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/psyche-self-observation-page.tsx`, `src/components/notes/note-tags-input.tsx`, `src/components/calendar/calendar-week-toolbar.tsx`, and `src/components/ui/select-menu.tsx` returns no matches. Live-verified `/forge/psyche/self-observation` at 1365px and 390px, including opening the add-observation sheet: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `/forge/psyche/screen-time` away from dark-only badges, white text, raw rgba panels, chart axes, chart fills, and dark tooltip styling. Route cards now use `--ui-surface-*`, `--ui-ink-*`, `--ui-border-*`, semantic soft surfaces, and theme-aware Recharts colors. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/psyche-screen-time-page.tsx` returns no matches. Live-verified `/forge/psyche/screen-time` at 1365px and 390px across day/month/all-time tabs: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `/forge/psyche/behaviors` remaining behavior-kind choice cards, behavior columns, behavior cards, descriptions, recovery text, and shared `EntityLinkMultiSelect` create-error styling away from hardcoded white/dark/rose utilities. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/psyche-behaviors-page.tsx`, `src/components/psyche/atlas-panel.tsx`, `src/components/psyche/entity-link-multiselect.tsx`, `src/components/psyche/schema-badge.tsx`, and `src/components/psyche/use-psyche-focus-target.ts` returns no matches. Live-verified `/forge/psyche/behaviors` at 1365px and 390px, including opening the guided Add behavior dialog: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `PsycheQuestionnaireBuilderPage`, covering `/forge/psyche/questionnaires/new` and `/forge/psyche/questionnaires/:instrumentId/edit`, away from dark gradients, raw rgba borders/backgrounds, white text, dark-only badges, and unbounded JSON editor chrome. Builder step tabs, metadata fields, select controls, JSON textareas, inline code, metric preview cards, draft posture, action card, and JSON error state now use `--ui-*` and semantic tokens with `min-w-0`, `max-w-full`, wrapping, and `overflow-wrap:anywhere` boundaries. Fixed a route-level reactivity bug where the builder effect depended on the whole mutation object, causing the new route to emit React maximum-update-depth errors. Source grep for hardcoded white/dark/raw rgba patterns in `src/pages/psyche-questionnaire-builder-page.tsx` returns no matches. Live-verified `/forge/psyche/questionnaires/new` at 1365px and 390px across Metadata, Structure, Scoring, and Publish steps: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders. Live-verified `/forge/psyche/questionnaires/:instrumentId/edit` at 1365px and 390px using a temporary custom questionnaire that was deleted after the check: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `PsycheQuestionnaireDetailPage` for `/forge/psyche/questionnaires/:instrumentId`, replacing dark gradients, white/raw-rgba cards, badges, provenance links, section rows, and history rows with `--ui-*` and semantic tokens plus shrink/wrap boundaries. Replaced the small Recharts history chart with a stable responsive SVG sparkline because live route checks exposed repeated Recharts hidden-container width/height warnings during route transition. Source grep for hardcoded white/dark/raw rgba patterns in `src/pages/psyche-questionnaire-detail-page.tsx` returns no matches. Live-verified `/forge/psyche/questionnaires/questionnaire_phq_9` at 1365px and 390px: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders, and the SVG history chart rendered.
- 2026-06-03: Tokenized `PsycheQuestionnaireRunDetailPage` for `/forge/psyche/questionnaire-runs/:runId`, replacing dark gradient result cards, white/raw-rgba score rows, answer ledger rows, and selected/unselected option badges with `--ui-*` and semantic success tokens plus `min-w-0`, `break-words`, and `overflow-wrap:anywhere` boundaries. Fixed `PsycheQuestionnaireRunPage` error-state ordering so a failed run start renders `ErrorState` instead of staying on the loading state forever. Source grep for hardcoded white/dark/raw rgba patterns in `src/pages/psyche-questionnaire-run-page.tsx` and `src/pages/psyche-questionnaire-run-detail-page.tsx` returns no matches. Live-verified `/forge/psyche/questionnaires/:instrumentId/take` at 1365px and 390px using a temporary custom questionnaire: prompt/options rendered, no failed responses, no console/page errors, `bodyOverflow = 0`; the temporary questionnaire was deleted. Live-verified `/forge/psyche/questionnaire-runs/:runId` at 1365px and 390px using a temporary completed run: stored scores and answer ledger rendered with long score/answer labels, no failed responses, no console/page errors, `bodyOverflow = 0`; the temporary questionnaire was deleted after verification.
- 2026-06-03: Tokenized `/forge/psyche/reports` route cards, report summary blocks, guided report row editors, and schema visual tones away from hardcoded dark/white/raw RGB styling. Fixed mobile report-card intrinsic overflow by adding `min-w-0`, wrapped text, two-line report headings, constrained action rows, and constrained summary cells. Also tokenized the shared shell user-scope trigger/avatar because it rendered as a dark-only `All` chip on the Paper light theme across this route. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/psyche-reports-page.tsx`, `src/components/psyche/report-chain-fields.tsx`, `src/lib/schema-visuals.ts`, `src/components/psyche/entity-link-multiselect.tsx`, `src/components/psyche/schema-badge.tsx`, and `src/components/psyche/atlas-panel.tsx` returns no matches. Live-verified `/forge/psyche/reports` in forced Paper light theme at 1365px and 390px, including opening the guided Reflect dialog: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders, and the shell `All` scope chip computed light-theme token colors.
- 2026-06-03: Tokenized `/forge/psyche/reports/:reportId`, shared `ChainCanvas`, `InsightFlowDialog`, and the shared notes delete control away from hardcoded dark/white/raw RGB styling. The chain stage rail now wraps responsively instead of relying on a horizontal scroll strip, and the detail editor select controls, schema/belief/mode/behavior pills, inspector panels, insights list, and insight target cards use `--ui-*` plus semantic success/warning/danger/info tokens. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/psyche-report-detail-page.tsx`, `src/components/psyche/report-chain-fields.tsx`, `src/components/psyche/chain-canvas.tsx`, `src/components/insights/insight-flow-dialog.tsx`, `src/lib/schema-visuals.ts`, `src/components/psyche/schema-badge.tsx`, `src/components/notes/entity-notes-surface.tsx`, and `src/components/knowledge-graph/open-in-graph-button.tsx` returns no matches. Live-verified `/forge/psyche/reports/trg_4c08c81800` in forced Paper light theme at 1365px and 390px, including the Lens stage and Store Insight dialog: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders.
- 2026-06-03: Tokenized `/forge/psyche/goal-map` hero actions, graph action button, inspector eyebrow/title/summary/stat/chip surfaces, fallback CTA, and shared `EntityName` pill shadow away from hardcoded white/raw RGB styling. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/psyche-goal-map-page.tsx`, `src/components/psyche/psyche-graph.tsx`, `src/components/psyche/psyche-section-nav.tsx`, `src/components/ui/card.tsx`, `src/components/ui/entity-badge.tsx`, and `src/components/ui/entity-name.tsx` returns no matches. Live-verified `/forge/psyche/goal-map` in forced Paper light theme at 1365px and 390px: visible graph frame rendered on both desktop and mobile, no failed responses, no console/page errors, `bodyOverflow = 0`, no HTML overflow offenders.
- 2026-06-03: Reopened `/forge/overview` because the previous `mobile checked` pass missed Paper light theme failures. Tokenized `OverviewPage` hardcoded dark/white utilities, the shared `GamificationOverviewWidget`/`GamificationMiniHud` dark-only chrome, `FlagshipSignalDeck`, and the overview-visible create/menu/modal primitives (`Button`, signal `Badge`, `QuestionFlowDialog` error block, `FloatingActionMenu`, and `CreateMenu`). This covered the Actions / Open what matters now deck, compact action cards, expanded action cards, the mobile Create sheet, modal button contrast, level chip, conic progress remainder, mascot card, celebration toast, XP notice, raw rgba shadows, and dark gradients. Source grep for hardcoded dark/white/raw RGB patterns in `src/pages/overview-page.tsx`, `src/components/gamification/gamification-widgets.tsx`, `src/components/experience/flagship-signal-deck.tsx`, `src/components/ui/floating-action-menu.tsx`, and `src/components/create-menu.tsx` returns no matches. Live-verified `/forge/overview` in forced Paper light theme at 1365px and 390px: no failed responses, no console/page errors, `bodyOverflow = 0`, no overflow offenders, and targeted computed-style plus screenshot checks confirmed the visible Actions deck, Add Mode guided modal, and mobile Create sheet render light surfaces with dark readable controls.
- 2026-06-03: Reopened `/forge/habits` because route-local habit dialogs and habit card chrome were still using dark-only modal backgrounds, white text, white badges, raw rgba shadows, and unreadable light-theme streak/status surfaces. Tokenized `HabitsPage` and `HabitDialog` modal surfaces, delete/history dialogs, first-screen stat cards, list-order panel, habit card streak tile, history strip, status badges, entity badges, note link, edit/delete controls, and Done/Missed/Resisted/Performed action buttons. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, raw `rgba(`, and raw `rgb(` in `src/pages/habits-page.tsx` and `src/components/habit-dialog.tsx` returns no matches. Live-verified `/forge/habits` in forced light theme at 390px: top cards, habit action buttons, New Habit modal, history modal, and delete modal rendered light surfaces with readable text/buttons; no console/page errors and `bodyOverflow = 0`.
- 2026-06-04: Reopened `/forge/sports` because the previous pass only checked mobile overflow, not Paper light-theme correctness. Tokenized `SportsPage` route-local cards, editor steps, filters, date controls, chart tooltips/axes/grids, evidence-quality panels, workout rows, metadata chips, browser cards, and empty states away from hardcoded dark/white/raw RGB styling. Added a measured chart viewport so Recharts charts mount only after their fixed-height frame has nonzero dimensions, eliminating repeated zero-size console warnings. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, and raw `rgb(` in `src/pages/sports-page.tsx` returns no matches. Live-verified `/forge/sports` in forced Paper light theme at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons.
- 2026-06-04: Reopened `/forge/kanban` from the unchecked inventory. Tokenized `KanbanPage` route-local summary/filter/search chrome and split-task modal, then tokenized the visible `ExecutionBoard` cards, lanes, in-progress clusters, trash dropzone, action controls, badges, and delete confirmation modal away from hardcoded dark/white utility styling. Source grep for route-local dark/white patterns in `src/pages/kanban-page.tsx` now returns no matches; `ExecutionBoard` only retains one raw blue progress gradient used as a data accent. Live-verified `/forge/kanban` in forced Paper light theme at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. The route remains partially open because `TaskDialog`/`ProjectDialog` shared modal flows are listed route components and still need their own modal-open checks/tokenization.
- 2026-06-04: Tokenized `TaskDialog` hierarchy-anchor picker, selected-anchor chips, search input, suggestion dropdown, assignee select, tracked-time note, tag chips, and evidence copy away from hardcoded dark/white/raw RGB styling. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, and raw `rgb(` in `src/components/task-dialog.tsx` now returns no matches, and `npx tsc --noEmit` passes. The live modal-open verification is still pending because the Kanban shell uses an internal scroll/action surface that needs a more targeted Playwright opener.
- 2026-06-04: Completed the `/forge/kanban` dialog verification. Live-opened a board task action menu and `TaskDialog` edit flow at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, and no overflow offenders. Enabling project cards initially exposed a `RangeError: Invalid time value` in `ProjectCardShell` because a full `updatedAt` timestamp was passed to a date-key formatter; `ExecutionBoard` now normalizes project timestamps to `YYYY-MM-DD` before calling `formatDate`. Live-opened the project-level board filter, project action menu, and `ProjectDialog` edit flow at 1365px and 390px after the fix: 16 project cards rendered, no failed requests, no console/page errors, `bodyOverflow = 0`, and no overflow offenders. Constrained `FlowField`, `FlowChoiceGrid`, and `ProjectDialog` goal cards with `min-w-0`/`max-w-full`/breakable text after the mobile ProjectDialog goal list initially forced a 383px intrinsic row inside a 390px viewport. The split-task modal remains source-tokenized but was not live-opened because the current live board data has no `splitSuggestion.shouldSplit` task.
- 2026-06-04: Rechecked stale detail-route rows that still said only `mobile overflow fixed`. Live-verified `/forge/projects/project_c65a26cd72` and `/forge/sports/workouts/workout_edea95cc61` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, and no dark panels. `/strategies/:strategyId` remains source checked only because `/api/v1/strategies` has no live strategy detail record to open.
- 2026-06-04: Reopened `/forge/calendar` against the user's Paper light-theme screenshots. Verified the route-local Calendar page in forced Paper light at 769px: Vite source-backed runtime served `/forge/@vite/client` and `/forge/src/main.tsx`, the visible week-view, Life Force, action, weekly status, and guided-action cards sampled as light token surfaces, no actual dark panels were detected, no console/page errors occurred, and `bodyOverflow = 0`. Tokenized shared calendar `CalendarSetupGuide`, `SchedulingRulesEditor`, `TaskSchedulingDialog`, and `WorkBlockFlowDialog` preview/weekday/color controls away from hardcoded `text-white`, `bg-white/[...]`, `border-white`, raw dark gradients, and raw rgba rings. Live-opened the `/forge/calendar` task blocking rules sheet in forced Paper light at 769px: the modal rendered light surfaces with readable select/chips, no failed requests, no console/page errors, and `bodyOverflow = 0`. Live-checked `/forge/settings/calendar` initial mobile surface at 390px in forced Paper light: no actual dark panels and `bodyOverflow = 0`. Calendar remained partial until the deeper event, timebox, and connection-flow modal checks below.
- 2026-06-04: Completed the Calendar/settings Calendar source pass by tokenizing `CalendarEventFlowDialog`, `TimeboxPlanningDialog`, `CalendarConnectionProviderStep`, `CalendarConnectionDiscoveryStep`, `CalendarConnectionReviewStep`, `CalendarConnectionCredentialsStep`, and `SettingsCalendarPage` away from hardcoded white/dark/raw RGB styling. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, raw `rgb(`, and `linear-gradient` across `src/components/calendar`, `src/pages/calendar-page.tsx`, and `src/pages/settings-calendar-page.tsx` now returns no matches. Live-verified forced Paper light with Playwright fallback because the in-app Browser backend was unavailable: `/forge/calendar` at 769px had no dark panels, low-contrast buttons, or overflow offenders; the Event Guide modal had no dark panels, low-contrast buttons, or overflow; the Planning Guide modal initially had a 12px task-picker intrinsic overflow, then `TimeboxPlanningDialog` was constrained with `min-w-0`, `max-w-full`, overflow clipping, and wrapped task title text, and the recheck showed `bodyOverflow = 0` with no overflow offenders. Live-verified `/forge/settings/calendar` at 390px and the Google credential guided modal: no dark panels, no low-contrast buttons, no failed requests, no console/page errors, and `bodyOverflow = 0`.
- 2026-06-04: Tokenized the Wiki batch covering `/forge/wiki`, `/forge/wiki/page/:slug`, `/forge/wiki/ingest-history`, `/forge/wiki/new`, and `/forge/wiki/edit/:pageId`. `WikiArticleMarkdown` now uses `--ui-*` ink, surface, border, code, link, admonition, directive, and infobox tokens instead of white-on-dark assumptions. `WikiEditorPage` now uses tokenized editor frames, helper dialogs, toolbar icons/tooltips, metadata drawer, markdown textarea, preview frame, space picker, tags/alias chips, and helper search results with `min-w-0`, breakable titles/slugs, and mobile-safe action rows. `WikiIngestModal` now uses tokenized modal overlay/content, candidate cards, review/mapping controls, search wells, upload cards, advanced settings, progress/log/source/review panels, and semantic warning/accent surfaces. Fixed `/forge/wiki/ingest-history` mobile overflow where desktop-style job rows and long source file paths forced a 782px intrinsic width; job cards now stack on mobile and break long source paths. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, raw `rgb(`, and `linear-gradient` across `src/pages/wiki-page.tsx`, `src/pages/wiki-ingest-history-page.tsx`, `src/pages/wiki-editor-page.tsx`, `src/components/wiki`, and `src/components/knowledge-graph/open-in-graph-button.tsx` returns no matches. Live-verified forced Paper light with Playwright fallback: `/forge/wiki`, `/forge/wiki/ingest-history`, `/forge/wiki/page/2026-04-22-completed-the-hyrox-daily-strength-habit-with-the-new-85-sec-wall-sit`, `/forge/wiki/new`, and `/forge/wiki/edit/note_9b927ca7d1` at 1365px and 390px had no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. Also live-opened the `/forge/wiki/ingest-history?ingest=1` ingest modal and `/forge/wiki/new` Wiki-link helper modal/metadata drawer at 390px with `bodyOverflow = 0`, no dark panels, and no overflow offenders.
- 2026-06-04: Tokenized `/forge/today` route-local metrics, calendar, focus, current-task, recovery-task, due-habit, empty-action, and compact Life Force card surfaces away from hardcoded dark/white utility styling. `TodayPage` now uses shared `--ui-*` text/surface/border/accent tokens plus `min-w-0`, wrapped titles/descriptions, constrained action buttons, and mobile-safe event/habit rows. `LifeForceTodayCard`'s `LifeForceCompact` and no-payload fallback now render tokenized labels, values, feedback, and mini-stat panels so the embedded Today widget reacts correctly to Paper light theme. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, raw `rgb(`, and `linear-gradient` in `src/pages/today-page.tsx` returns no route-local matches. Live-verified `/forge/today` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/workbench` route-local flow metadata strip away from `text-white/44`, added `min-w-0` and breakable flow metadata, and tokenized shared `GenericWorkbenchNodeView` box-card, port list, empty state, handle, schema preview, and node chrome away from hardcoded dark gradients, white text, raw rgba shadows, and `bg-black` preview panels. Source grep for hardcoded white/dark/raw RGB patterns in `src/pages/workbench-page.tsx` and `src/components/workbench-boxes/shared/generic-node-view.tsx` returns no matches. Live-verified `/forge/workbench` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no dark panels, and no low-contrast buttons. Also live-verified representative `/forge/workbench/aic_d803a26ac2` at 390px and opened the Add node modal: no failed requests, no console/page errors, `bodyOverflow = 0`, no dark panels, and no low-contrast buttons. `/workbench/:flowId` remains source-open because deeper `WorkbenchFlowEditor` edit/run/history/detail sections and `workbench-dialogs` still contain hardcoded dark utility styles that require a full component pass before marking the route complete.
- 2026-06-04: Completed the Workbench flow pass by tokenizing `WorkbenchFlowEditor` and `workbench-dialogs` edit/run/history/detail surfaces away from hardcoded white/dark/raw RGB styling. The edit-node tool cards, enabled-tool/output previews, danger zone, run inspector, run history, node result cards, published outputs, debug details, flow settings id panel, run input panel, debug checkbox, recent run cards, add-node type buttons, and box result cards now use `--ui-*` ink/surface/border/code tokens. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, raw `rgb(`, and `linear-gradient` across `src/pages/workbench-page.tsx`, `src/pages/workbench-flow-page.tsx`, `src/components/workbench`, and `src/components/workbench-boxes/shared/generic-node-view.tsx` now only returns the intentional `WorkbenchNodeCard` tokenized primary accent gradient. Live-verified `/forge/workbench/aic_d803a26ac2` in forced Paper light at 390px, including initial page, Add node, Flow settings, Run flow, and Edit node dialogs: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/activity` route summary cards and `ActivityTable` away from hardcoded white/dark utility styling. The activity table now renders as a desktop table at `md+` and as real mobile cards below `md`, with breakable event titles/descriptions, tokenized source/date/action text, tokenized owner/empty states, and constrained remove/open controls. Grouped day cards now use `--ui-*` surface, border, ink, and accent tokens for highlighted events. Source grep for hardcoded `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, and raw RGB/RGBA in `src/pages/activity-page.tsx` and `src/components/activity-table.tsx` returns no dark-theme matches; the only hit is a tokenized `color-mix(in_srgb, var(--primary)...)` highlight shadow. Live-verified `/forge/activity` with real 100-event data in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/insights` route cards, heatmap cells, coaching panels, stored insight cards, status badges, and the shared `ApplyInsightDialog` selection cards/tag chips away from hardcoded white/dark/raw RGB styling. Replaced the route-local Recharts execution trend chart with a stable responsive SVG sparkline to eliminate repeated zero-size chart warnings during mobile route verification. Source grep for `recharts`, `ResponsiveContainer`, `AreaChart`, `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw `rgba(`, raw `rgb(`, and raw hex colors in `src/pages/insights-page.tsx` and `src/components/insights/apply-insight-dialog.tsx` returns no route-chrome matches; the only remaining hits are stored entity theme colors in `insight-apply-helpers`. Live-verified `/forge/insights` in forced Paper light at 1365px and 390px, including Store Insight and Apply Insight guided modals: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/review/weekly` route-local momentum, calibration, wins, reward, and completion panels away from hardcoded dark/white utility styling, and replaced the Recharts weekly XP chart with a stable responsive SVG chart so hidden or mobile containers cannot emit zero-size chart warnings. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw RGB/RGBA, raw hex colors, `ResponsiveContainer`, `BarChart`, and `recharts` in `src/pages/weekly-review-page.tsx` returns no matches. Live-verified `/forge/review/weekly` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Reopened `/forge/settings` for Paper light-theme correctness. Tokenized `SettingsPage`, `SettingsSectionNav`, and `ThemeCustomizerDialog` route-visible profile, execution policy, notification, theme calibration, gamification style, locale, mobile companion, operator session, integrity tooltip, Doctor, issue row, settings section picker, and custom-theme modal surfaces away from hardcoded dark/white utility styling. Source grep for `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw RGB/RGBA, and raw hex colors in `src/pages/settings-page.tsx`, `src/components/settings/theme-customizer-dialog.tsx`, and `src/components/settings/settings-section-nav.tsx` returns only the intentional `#7cc7ff` placeholder for custom color input. Live-verified `/forge/settings` in forced Paper light at 1365px and 390px, plus mobile Settings section picker and custom-theme guided modal: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/data` remaining backup, recovery, candidate, restore, safety, session, and integrity panels away from hardcoded dark/white and fixed Tailwind amber/emerald styling. Added mobile shrink/wrap boundaries to the live data-root flex header and data fact cards so long local paths cannot expand the first card or metric grids beyond the mobile viewport. Source grep for hardcoded white/dark/raw RGB and fixed semantic color utilities in `src/pages/settings-data-page.tsx` returns no matches. Live-verified `/forge/settings/data` in forced Paper light at 1365px and 390px, plus the mobile Move data and Restore backup guided modals: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/users` and shared user relationship components away from hardcoded dark/white route chrome. The users page, user cards, search/filter panel, multi-user posture panels, agent onboarding card, `UserRelationshipGraph`, `UserSettingsFlowDialog`, and `UserRelationshipFlowDialog` now use `--ui-*` ink, surface, border, accent, success, warning, and danger tokens. Tightened the relationship graph's mobile canvas height and fit padding so it behaves as a contained component instead of an oversized sparse graph. Source grep for hardcoded dark/white/raw RGB patterns in `src/pages/settings-users-page.tsx` and `src/components/users` returns only intentional user accent fallback hex values and a tokenized graph gradient. Live-verified `/forge/settings/users` in forced Paper light at 1365px and 390px, plus the mobile Create user and relationship modals: no failed requests, no console/page errors, `bodyOverflow = 0`, no non-graph overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/mobile` route-local QA, pairing, QR, payload, companion-state, active-pairing, source-toggle, action-link, and sync-history surfaces away from hardcoded dark/white styling. Added explicit `min-w-0`, `max-w-full`, `overflow-hidden`, and break rules around active pairing rows so long `forge-iroh://` API URLs and pairing/session ids wrap inside the mobile card instead of widening the route. Source grep for hardcoded dark/white/raw RGB patterns in `src/pages/settings-mobile-page.tsx` returns only switch `translate-x-*` layout utilities. Live-verified `/forge/settings/mobile` in forced Paper light at 1365px and 390px, including the mobile QR/payload path during verification: no failed requests, no console/page errors, `bodyOverflow = 0`, no visible overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/mobile/lab` dev QA fixtures away from dark-only cards, white text, raw dark gradients, black panels, and fixed Tailwind amber/emerald/rose/pink badge colors. Source matrix, gap classifier, coverage preview, timeline segment rows, canonical box-layer columns, raw/automatic/user/projected cards, uncovered intervals, and semantic badges now use `--ui-*` and semantic success/warning/danger/accent tokens. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in `src/pages/companion-sync-lab-page.tsx` returns no matches. Live-verified `/forge/settings/mobile/lab` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/models` route-local Forge Agent defaults, KarpaWiki embedding profiles, connection editor, OpenAI Codex OAuth panel, mock-runtime panel, feedback messages, connected-agent cards, inputs, selects, provider buttons, status badges, empty/error panels, and long model/base URL/account labels away from hardcoded dark/white styling. Added `min-w-0`, breakable text, and wrapped badges around connection URLs and model metadata so saved provider rows cannot widen the mobile viewport. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in `src/pages/settings-models-page.tsx` returns no matches. Live-verified `/forge/settings/models` in forced Paper light at 1365px and 390px, plus the mobile OpenAI Codex OAuth and Workbench mock provider panels: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/rewards` reward-operation chrome and shared `XpCommandDeck` away from hardcoded dark panels, white text, raw RGB gradients, and dark inset rows. Reward rule editor, manual bonus form, manual bonus history, XP deck header, achievement cards, milestone cards, and XP ledger rows now use `--ui-*` ink/surface/border/accent/success/warning tokens with mobile-safe wrapping. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in `src/pages/settings-rewards-page.tsx` and `src/components/xp/xp-command-deck.tsx` returns no matches. Live-verified `/forge/settings/rewards` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/rewards` trophy hall filters, reward tiles, near-completion cards, armory equipment panel, unlock cards, streak state cards, category headings, and reward detail modal away from hardcoded dark/white/raw RGB styling. Added mobile-safe search sizing, modal scrolling, wrapped reward metadata, and semantic tier/status badges. Source grep for route-local hardcoded dark/white/raw RGB patterns in `src/pages/rewards-page.tsx` returns no dark-theme matches. Live-verified `/forge/rewards` in forced Paper light at 1365px and 390px, including Armory, Streak Forge, and reward detail modal states: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/wiki` model handoff card, model-settings links, space rows, create-space panel, and visibility select away from hardcoded dark/white utility styling. Added `min-w-0` and breakable slug/label handling so SQLite namespace identifiers cannot widen the mobile viewport. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in `src/pages/settings-wiki-page.tsx` returns no matches. Live-verified `/forge/settings/wiki` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/logs` diagnostic filter controls, anchored multiselect popover, selected chips, level/source/scope badges, virtualized log row cards, structured-detail panels, empty state, and scroll footer away from hardcoded dark/white/raw RGB styling. Added breakable log messages, event keys, detail rows, and ids so long diagnostics cannot widen mobile. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in `src/pages/settings-logs-page.tsx` returns no route-style matches. Live-verified `/forge/settings/logs` in forced Paper light at 1365px and 390px, including opening the Levels filter menu on both viewports: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/settings/bin` search/filter controls, entity-type pills, visible-count summary, empty states, group headers, fallback entity chips, deleted-item rows, titles, subtitles, and deletion metadata away from hardcoded dark/white utility styling. Added breakable titles, subtitles, reasons, actors, sources, and ids so deleted records cannot widen mobile cards. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in `src/pages/settings-bin-page.tsx` returns no route-style matches. Live-verified `/forge/settings/bin` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Tokenized `/forge/tasks/:taskId` route-local task detail cards, status controls, badges, execution brief, evidence panels, AP/Life Force/timer/scheduling sections, scheduled blocks, activity rows, timer sessions, `TaskDetailContextGrid`, `TaskDetailPage` helper cards, and `WorkAdjustmentDialog` preview away from hardcoded dark/white/raw RGB styling. Tightened `SheetScaffold`, `TaskSchedulingDialog`, and `SchedulingRulesEditor` to clamp mobile sheets and nested selects/cards to `calc(100vw - 2rem)` with `min-w-0`/`max-w-full` so scheduling-rule sheets cannot create 427px-wide content in a 390px viewport. Source grep for hardcoded dark/white/raw RGB and fixed color utilities in the task detail route files returns no route-style matches. Live-verified `/forge/tasks/task_46d4e7129a` in forced Paper light at 1365px and 390px, including status sheet, edit task dialog, adjust work dialog, timebox dialog, and task scheduling dialog: no failed requests, no console/page errors, `bodyOverflow = 0`, no dark panels, no low-contrast buttons, and after the sheet clamp no mobile overflow offenders. `npx tsc --noEmit` passes.
- 2026-06-04: Live-verified `/forge/strategies/:strategyId` by creating a temporary strategy fixture, then tokenized `StrategyGraphCanvas` and `StrategyHierarchyTree` after the first scan exposed hardcoded dark graph/hierarchy cards (`text-white`, `border-white`, raw dark `rgba`, fixed emerald/rose/amber/sky badge tones) and a mobile React Flow node sizing issue. The graph canvas now uses `--ui-*` surfaces, borders, ink, shadows, semantic soft tones, and a width-clamped node body; the hierarchy tree now uses tokenized phase cards, node cards, dependency chips, and wrapped title/note text. Source grep for hardcoded dark/white/raw RGB patterns in those two strategy components returns no matches. Re-verified the temporary `/forge/strategies/strategy_8cd332752b` route in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no route overflow offenders outside React Flow transform internals, no dark panels, and the visible React Flow node was 294px wide on mobile. Removed the temporary strategy fixture from the live Forge SQLite store after backing up the database to `forge.sqlite.audit-strategy-cleanup.20260604T033634Z.bak`; `/api/v1/strategies` then returned an empty strategy list. `npx tsc --noEmit` passes.
- 2026-06-04: Reopened `/forge/knowledge-graph` because the previous status only proved mobile and source grep still showed dark desktop toolbar, advanced filters, graph dialogs, entity panel, force-view wrapper, and hierarchy lane/node chrome. Tokenized the page graph surface, renderer fallback, desktop toolbar count pill/buttons, advanced filter popover, graph appearance dialog, diagnostics dialog, focused entity panel, force-view fallback SVG chrome, and hierarchy React Flow nodes/lane labels with `--ui-*` ink/surface/border/shadow tokens plus mobile node width clamps. Source grep for hardcoded `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw RGB/RGBA, raw gradients, and raw dark shadows across `src/pages/knowledge-graph-page.tsx`, `KnowledgeGraphEntityPanel`, `KnowledgeGraphHierarchyView`, and `KnowledgeGraphForceView` now returns only Sigma's intentional internal `defaultEdgeColor`. Live-verified `/forge/knowledge-graph` in forced Paper light at 1365px and 390px, including main graph, appearance dialog, desktop advanced filters, and hierarchy mode: no console/page errors, no user-visible failed requests aside from the expected aborted SSE stream when closing the test browser, `bodyOverflow = 0`, no dark panels, and no low-contrast buttons. The hierarchy lane strip still contains offscreen lane chips by design inside its own `overflow-x-auto` graph-canvas header; it does not widen the body viewport. `npx tsc --noEmit` passes.
- 2026-06-04: Reopened `/forge/life-force` because `LifeForceOverviewWorkspace` still had many hardcoded white text utilities, translucent white fills, raw dark chart tooltip colors, and raw SVG/Recharts colors despite the route being marked only mobile-checked. Tokenized header cards, stat strips, editor copy, weekday controls, chart frame, chart axes/grid/cursor/tooltip, drag ghost, draggable handles, templates/drains sections, task badges, and summary text with `--ui-*` ink/surface/border/shadow/accent tokens. Source grep for hardcoded `text-white`, `bg-white`, `border-white`, `white/`, `bg-black`, `text-black`, raw RGB/RGBA, raw gradients, and raw dark shadows in `src/components/life-force/life-force-workspace.tsx` returns no matches. Live-verified `/forge/life-force` in forced Paper light at 1365px and 390px: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
- 2026-06-04: Reopened `/forge/goals` because `GoalsPage`, `GoalStudio`, and `GoalDialog` still used white/dark utilities, translucent white cards, raw dark gradients, and fixed emerald/amber status text. Tokenized goal action pill, goal cards, inline metrics, project summary panels, empty states, studio intro/goal cards, edit icon button, badges, progress rail, momentum footer, and goal-dialog color/tag controls with `--ui-*` and semantic success/warning tokens. Source grep for hardcoded white/dark/raw RGB/gradient patterns and fixed emerald/amber text in `src/pages/goals-page.tsx`, `src/components/goal-studio.tsx`, and `src/components/goal-dialog.tsx` returns no matches. Live-verified `/forge/goals` in forced Paper light at 1365px and 390px, including the Create goal guided dialog: no failed requests, no console/page errors, `bodyOverflow = 0`, no overflow offenders, no dark panels, and no low-contrast buttons. `npx tsc --noEmit` passes.
