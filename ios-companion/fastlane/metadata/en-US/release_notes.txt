Forge Companion 1.0.93 improves HealthKit sync recovery and Forge dashboard load speed.

HealthKit chunk uploads can now recover by resuming a compatible accepted session even when the phone has to reconnect without an explicit resume id, reducing repeated historical uploads.

The embedded Forge overview also uses the compact health payload so the app becomes usable faster while detailed workout history remains available from the Sports view.
