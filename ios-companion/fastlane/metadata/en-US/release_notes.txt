Forge Companion 1.0.2 improves sync resilience when Apple Health returns no matching
data for a query. The iOS companion now treats those responses as empty results instead
of failing the entire sync, reducing repeated resync prompts and fixing the predicate
error shown in Companion status.
