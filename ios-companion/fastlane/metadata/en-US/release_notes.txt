Forge Companion 1.0.102 improves foreground HealthKit evidence throughput.

Foreground HTTP sync now prepares larger HealthKit chunks so active uploads spend less time waiting on many small requests.

Background and Iroh transfers keep the conservative chunk size while active app sync can move more workout time-series and route evidence per request.
