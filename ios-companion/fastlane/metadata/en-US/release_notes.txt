Forge Companion 1.0.116 keeps the Iroh bridge warm during foreground HealthKit sync.

Foreground Iroh uploads now reuse the bridge runtime, endpoint, and connection, so concurrent chunks open lightweight streams instead of rebuilding the transport for every request.

Background transfers stay conservative, and stale Iroh connections are dropped automatically after stream failures so the next retry reconnects cleanly.
