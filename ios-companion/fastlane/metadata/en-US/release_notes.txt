Forge Companion 1.0.86 fixes stale and partially applied Forge themes inside the embedded web view.

The native wrapper no longer overrides Forge’s web theme backgrounds, so the current Forge theme controls page backgrounds and cards consistently.

The Refresh Forge cache action now clears modern WebKit fetch and service-worker caches while preserving pairing credentials and local app preferences.
