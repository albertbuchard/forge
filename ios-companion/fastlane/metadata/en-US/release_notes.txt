Forge Companion 1.0.85 fixes physical-phone pairing when Forge Memory cannot create a reachable transport.

The app now rejects localhost-only pairing payloads on real iPhones instead of getting stuck after scanning a QR code.

QR scanning also uses the same compact/full payload decoder as manual setup, so old and new Forge Memory payload formats are handled consistently.
