Forge Companion 1.0.98 improves foreground HealthKit sync diagnostics.

The sync view now distinguishes completed chunk acknowledgements from chunks that are already scheduled and in flight.

This makes foreground HealthKit upload stalls easier to diagnose on device: when an acknowledgement is delayed, the app can show whether the upload pipe is still active or empty.
