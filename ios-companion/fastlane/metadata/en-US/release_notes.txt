Forge Companion 1.0.87 improves HealthKit sync recovery after Iroh transport timeouts.

Health sync chunk uploads now get a longer Iroh response window, which helps larger movement batches complete after reconnecting or repairing a pairing.

The native Iroh bridge also handles timeout/error responses more defensively, so retries surface the real transport error instead of degrading into a generic missing-data failure.
