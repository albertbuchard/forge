Forge Companion 1.0.149 repairs stale transport metadata for existing Tailscale pairings.

If a previous pairing saved an Iroh transport object while the active API URL is a Tailscale HTTPS URL, the app now rewrites the stored pairing back to Tailscale direct during session refresh.

Watch direct-sync metrics now share the same tested model as the iPhone app, so Tailscale backup wording stays clear and avoids queue/Iroh labels when Tailscale is the active route.

Pairing tokens, upload checkpoints, HealthKit data, watch action persistence, and stored user data are unchanged.
