Forge Companion 1.0.22 merges touching canonical stays that belong to the same place,
so the paired timeline stops fragmenting one continuous visit into duplicate blocks.
Shared raw stay identifiers are now preserved across coalesced stays, which keeps
place relabeling and stay continuity stable after sync repairs.
