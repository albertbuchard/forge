Forge Companion 1.0.12 fixes iPhone-created movement places so unlabeled stays
can be turned into saved locations without the API rejecting an empty
externalUid. This release also keeps the expanded on-device diagnostics surface
so failed labeling flows remain copyable from Diagnostics > Logs when anything
else goes wrong.
