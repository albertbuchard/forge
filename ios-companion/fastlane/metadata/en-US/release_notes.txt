Forge Companion 1.0.118 increases foreground HealthKit sync throughput over the Iroh bridge.

Foreground Iroh uploads now keep up to six HealthKit chunks in flight instead of three, doubling the modeled upload wave from 3 MB to 6 MB while keeping background uploads serialized for durability.

The scheduler test now verifies the same 18-chunk workout evidence batch drains in three foreground waves instead of six under the previous cap.
