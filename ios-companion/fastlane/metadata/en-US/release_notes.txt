Forge Companion 1.0.96 improves HealthKit sync upload responsiveness.

Foreground HealthKit sync now uses the fast standard URLSession upload path instead of forcing every chunk through the iOS background-transfer scheduler.

Background and inactive sync still keep the durable background upload path, while logs now show the selected chunk upload transport for easier diagnosis.
