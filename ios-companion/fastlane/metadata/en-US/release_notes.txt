Forge Companion 1.0.99 improves foreground HealthKit sync throughput.

Sleep, vitals, movement, and screen-time chunks are now prepared together before foreground upload dispatch.

This lets the app keep the foreground upload window fuller instead of waiting for each base data family to finish before scheduling the next one.
