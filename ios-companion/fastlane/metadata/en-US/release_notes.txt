Forge Companion 1.0.14 finishes the stay relabeling fix by updating the live
mobile movement backend path used by the iPhone app, so selecting an existing
saved location or creating a new one can now attach to recorded stays instead
of failing with the old immutable-stay error. Diagnostics log controls and
location-distance warnings remain included from the previous release.
