Forge Companion 1.0.94 improves HealthKit sync recovery and Forge dashboard load speed.

HealthKit chunk uploads can now recover by resuming a compatible accepted session even when the phone has to reconnect without an explicit resume id, reducing repeated historical uploads.

Sync completion now avoids rereading already-applied chunk payloads, so large historical HealthKit sessions finish faster on Forge while preserving resumable recovery.
