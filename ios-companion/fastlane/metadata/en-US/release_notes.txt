Forge Companion 1.0.97 improves foreground HealthKit sync throughput.

Foreground HealthKit sync can now keep several prepared HTTP chunks in flight instead of waiting for every chunk acknowledgement before starting the next upload.

Background and Iroh sync stay conservative, while the foreground HTTP path uses a larger connection window and logs the active upload window for diagnosis.
