Forge Companion 1.0.115 improves foreground HealthKit upload throughput over the Iroh tunnel.

Foreground Iroh sync can now prepare chunks ahead of time and keep up to three upload requests in flight instead of serializing one request at a time.

The sync panel now labels whether the active transfer is using HTTP or the Iroh tunnel, making slow waits easier to diagnose.
