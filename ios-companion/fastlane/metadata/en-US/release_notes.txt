Forge Companion 1.0.34 hardens HealthKit sync on iPhone. The app now guards workout
and vital quantity conversions against incompatible units, so unexpected HealthKit
payloads are skipped safely instead of crashing during sync.
