Forge Companion 1.0.72 ships the latest native sync and agent-runtime release
metadata alongside the updated Forge plugin runtime.

Historical workout import status remains in the native iOS menu and Settings,
not as an overlay above the Forge web app.

The app keeps the clamped historical workout progress display and current raw
workout evidence contract for heart-rate and route replay.
