Forge Companion 1.0.112 makes HealthKit sync progress easier to diagnose while uploads are active.

The sync panel now separates accepted throughput from active requests, shows how many bytes are still waiting for Forge replies, and names the oldest in-flight request age.

This prevents a misleading 0 B/s display when the phone has already sent a chunk and is waiting on the network or Forge backend to respond.
