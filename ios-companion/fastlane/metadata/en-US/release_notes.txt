Forge Companion 1.0.101 improves foreground HealthKit upload throughput.

Foreground chunk uploads now use the full six-connection HTTP window instead of stopping at four concurrent chunks.

Background and Iroh transfers remain conservative while active app sync can keep more prepared packets in flight.
