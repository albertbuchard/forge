-- Marker migration. initializeDatabase conditionally restores the final
-- additive 087/088 People schema for installations that applied earlier
-- published revisions of those migration files.
SELECT 1;
